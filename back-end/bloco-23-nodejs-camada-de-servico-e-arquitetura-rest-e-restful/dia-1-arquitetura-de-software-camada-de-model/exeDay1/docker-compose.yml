version: '3.1'
services:
  api:
    image: node:16
    restart: always
    ports:
      - 4000:3000
    volumes:
      - ./:/usr/app
    working_dir: /usr/app
    command: bash -c "npm install && npm start"